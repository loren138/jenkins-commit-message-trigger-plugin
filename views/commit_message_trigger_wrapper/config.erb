<%
f = taglib("/lib/form")
f.block do
  f.entry(:title => 'Keyword',
          :field => 'build_text',
          :description => 'This must appear somewhere in the commit message as [ci KEYWORD] case insensitive for a build to progress.') do
    f.textbox
  end
end
%>
